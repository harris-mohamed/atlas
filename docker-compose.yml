services:
  war-room-bot:
    build: .
    container_name: atlas-war-room
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
      - ./logs:/app/logs
    networks:
      - atlas-network

networks:
  atlas-network:
    driver: bridge
