# Atlas Environment Variables

# Discord Bot Configuration
DISCORD_TOKEN=                    # Discord bot token (required)
DISCORD_CONTROL_CHANNEL_ID=       # Control channel ID for admin interface (required)

# Claude API
ANTHROPIC_API_KEY=                # Claude API key (required)

# OneCLI Agent Vault (credential gateway for containers)
ONECLI_URL=http://127.0.0.1:4444  # Run /init-onecli to set up

# GitHub Integration (for /build command — migrate to vault via /init-onecli)
GITHUB_TOKEN=                     # GitHub personal access token (optional, for PR creation)

# Brave Search (for report agents — migrate to vault via /init-onecli)
BRAVE_API_KEY=                    # Brave Search API key (optional)

# Logging
LOG_LEVEL=info                    # Options: trace, debug, info, warn, error

# Timezone
TZ=America/New_York               # Your timezone for scheduled tasks
